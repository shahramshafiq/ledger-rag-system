venv/
__pycache__/
*.pyc
.env

# raw downloaded filings are large and regenerable via download_filings.py, not source
data/filings/

# files uploaded through POST /documents are runtime artifacts, not source
data/uploads/
