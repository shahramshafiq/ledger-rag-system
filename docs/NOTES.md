Baseline (structure-aware chunking, dense retrieval, top-5, no reranking or filtering): 11/25 questions correct overall. Retrieval itself is not the bottleneck, recall@5 was 100% on every question where a specific source document could be checked. The baseline handles direct single-fact lookups well (4/5) but fails badly on anything requiring computation from retrieved numbers (2/5 on table-derived values like operating margin), synthesis across multiple documents (1/4 on cross-document questions), or corpus-wide summarization (0/3 on broad questions). It correctly declined on 3 of 4 unanswerable questions.

